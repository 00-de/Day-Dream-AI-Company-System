# 外部サービス連携について

## 結論から：サービスごとに連携できる範囲が違います

| サービス | 公開API | アプリ内で生成 | 連携方法 |
| --- | --- | --- | --- |
| **ChatGPT**（OpenAI） | あり | できる | `OPENAI_API_KEY` を登録すれば即利用 |
| **Claude**（Anthropic） | あり | できる | `ANTHROPIC_API_KEY` を登録すれば即利用 |
| **Suno** | なし | できない | プロンプトを作って開く（半自動） |
| **Genspark** | なし | できない | プロンプトを作って開く（半自動） |
| **CapCut** | なし | できない | 素材リストを渡す（半自動） |

Suno・Genspark・CapCut には一般向けの公開APIがありません。
ネット上に「Suno API」を名乗るサービスがありますが、**すべて非公式**です。
規約違反でアカウント停止のおそれがあり、予告なく使えなくなるため採用していません。

---

## 半自動連携の流れ

制作画面の一番上に **「外部サービス連携」** パネルがあります。

1. **何を作るか選ぶ** … 楽曲を作る / 画像を作る / MVを編集する / 歌詞を作る
2. **思いつきを入力** … 「夏の海で全力疾走するような明るいロック」程度でOK
3. **「指示文をAIに作らせる」を押す**
   AIが、そのサービス専用の形に整えた指示文を作ります
4. **「指示文をコピーして ◯◯ を開く」を押す**
   クリップボードにコピーされ、新しいタブでサービスが開きます
5. サービス側で貼り付けて生成
6. できたファイルを、アプリのアップロード枠にドラッグ＆ドロップ

### なぜこの形が実用的か

一番時間がかかるのは「良いプロンプトを考えること」です。そこをAIに任せます。

- **Suno** … 歌詞は日本語、Style欄は英語で、と使い分けます（Sunoは英語のほうが正確に解釈します）。`[Verse 1]` `[Chorus]` の構成タグも自動で入ります
- **Genspark** … 画像生成は英語のほうが精度が高いので、英語プロンプトと日本語訳の両方を出します
- **CapCut** … 4分の曲を8〜12個に区切った構成表と、実際にCapCutにある機能名で編集指示を出します
- **歌詞** … 5人（悠真・葵・蓮・結衣・大地）のパート割りまで提案します

---

## CapCutへの素材の渡し方

連携パネルの一番下に「素材リストを保存」ボタンがあります。

アップロード済みの楽曲・画像・動画のダウンロードURLをまとめたテキストが保存されるので、
それを見ながらCapCutに読み込んでください。

---

## Claude APIを追加する手順

1. https://console.anthropic.com/settings/keys でAPIキーを作成
2. Vercel の Settings → Environment Variables に登録

   | Key | Value |
   | --- | --- |
   | `ANTHROPIC_API_KEY` | 作成したキー |

3. **Redeploy**

`VITE_` は付けないでください。

### 接続の優先順

**Groq → Gemini → OpenAI → Claude**

上から順に試し、最初に成功したものを使います。
Groqが最速で無料枠も大きいので、普段はGroqが応答します。
Claudeは、長い歌詞や複雑な構成表を作るときに品質が高いので、
Claudeを優先したい場合は他のキーを一時的に消してください。

---

## 将来API連携できる可能性があるもの

- **YouTube Data API** … 登録者数・再生回数の自動取得は技術的に可能です（要OAuth設定）
- **ElevenLabs** … 音声読み上げの公式APIあり
- **Stability AI / Replicate** … 画像生成の公式APIあり。Gensparkの代わりにアプリ内生成を実現できます

「アプリの中で画像生成まで完結させたい」場合は、Gensparkの代わりに
Stability AI などの公式APIを使う形になります。ご希望があれば実装できます。
