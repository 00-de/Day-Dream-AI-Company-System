# AIの使い分け設定

OpenAI・Groq・Gemini・Claude を、目的に合わせて使い分けられます。
すべてVercelの環境変数だけで設定でき、コードの変更は不要です。

---

## 何も設定しない場合

**Groq → Gemini → OpenAI → Claude** の順に試し、最初に成功したものを使います。
登録していないものは飛ばされます。これでも十分動きます。

---

## ① 交互に使う（無料枠を分け合う）

| Key | Value |
| --- | --- |
| `AI_ROTATE` | `1` |

呼び出しごとに先頭がずれます。

```
1回目： Groq → Gemini → OpenAI
2回目： Gemini → OpenAI → Groq
3回目： OpenAI → Groq → Gemini
4回目： Groq → Gemini → OpenAI（以下くり返し）
```

**向いている場面**：Groqの無料枠とGeminiの無料枠を両方使い切りたいとき。
1つに偏らないので、どれかが上限に達しても他が残ります。

**注意**：先頭が変わっても、失敗すれば次に回ります。止まることはありません。

---

## ② 順番を指定する

| Key | Value |
| --- | --- |
| `AI_ORDER` | `openai,groq,gemini` |

指定した順に試します。名前は `groq` `gemini` `openai` `anthropic` を使います。
カンマ区切りで、間に空白が入っても構いません。

**向いている場面**：OpenAIに課金済みで、まずそれを使いたいとき。
残高切れのGeminiを後ろに回したいとき。

---

## ③ 重い処理だけ別のAIにする（おすすめ）

| Key | Value |
| --- | --- |
| `AI_HEAVY` | `openai` |

**会議・全社員の意見・調査・絵コンテ・レポート・開発相談**のとき、
指定したAIを最優先にします。チャットなど軽い処理は通常の順番のままです。

**なぜこれが良いか**

処理によって重さがまったく違います。

| 処理 | 送るデータ量 |
| --- | --- |
| AI秘書チャット | 数百字 |
| SNS投稿文・チェック | 1〜2千字 |
| **調査（陽太AI・知也AI）** | **約1万字** |
| **会議・絵コンテ** | **数千〜1万字** |

重い処理は無料枠をすぐ使い切ります。そこで、

- **軽い処理** → 無料のGroq・Geminiに任せる
- **重い処理** → 課金済みのOpenAIに任せる

とすれば、費用を抑えつつ安定します。

---

## おすすめの設定（今のトシさんの状況）

OpenAIに $2.16 の残高があり、Geminiは残高切れ、Groqはモデル名が古い状態ですね。

**まずはこれだけ**

| Key | Value |
| --- | --- |
| `AI_ORDER` | `openai` |

OpenAIだけを使います。確実に動きます。

**Groqのモデル名を直したあとは**

| Key | Value |
| --- | --- |
| `AI_ORDER` | `groq,openai` |
| `AI_HEAVY` | `openai` |

軽い処理は無料のGroq、重い処理は安定したOpenAI。費用が最小になります。

**Geminiにも課金する場合**

| Key | Value |
| --- | --- |
| `AI_ROTATE` | `1` |
| `AI_HEAVY` | `openai` |

3つの枠を均等に使いながら、重い処理だけOpenAIに寄せます。

---

## 設定の確認方法

アプリの **設定 → AI接続** に、次が表示されます。

- **使えるAI**：`OpenAI（gpt-4o-mini）` のようにモデル名まで
- **使い分け**：交互に使用／上から順に使用
- **次に使う順**：`OpenAI → Groq → Gemini`
- **重い処理**：`openai を優先`（設定時のみ）

設定後は必ず **Redeploy** してください。環境変数は再デプロイで反映されます。

---

## 注意点

`AI_ROTATE` の順番は、Vercelのサーバーが起きている間だけ保持されます。
しばらく使わないとサーバーが停止し、次回は最初の順番に戻ります。
厳密に均等にはなりませんが、偏りを減らす効果はあります。
