# フェーズ4：ファイルアップロードの設定

画像・楽曲・動画をアプリから直接アップロードして、みんなで共有できるようになります。

---

## 手順1：Firebase Storage を有効にする

1. [Firebaseコンソール](https://console.firebase.google.com/) でプロジェクトを開く
2. 左メニューの **Storage** をクリック
3. **「始める」** を押す
4. ロケーションを選ぶ（**asia-northeast1（東京）** がおすすめです）
   ※ ロケーションは後から変更できません
5. セキュリティルールは、いったんそのまま「次へ」で進めてOKです（次の手順で入れ替えます）

---

## 手順2：セキュリティルールを入れる

1. Storage の画面の上にある **「ルール」** タブを開く
2. 中身をすべて消して、同梱の **`storage.rules`** の内容を貼り付ける
3. **「公開」** を押す

これで、登録した5人以外はファイルに触れなくなります。

| 役割 | できること |
| --- | --- |
| 社長・高木さん・太田さん | アップロード・表示・削除 |
| 中尾さん・シュンさん | 表示のみ |
| それ以外 | 何もできません |

---

## 手順3：Firestore のルールも更新する

タスク管理用のルールが増えているので、こちらも入れ替えてください。

1. Firestore Database → **ルール** タブ
2. 同梱の **`firestore.rules`** を貼り付けて「公開」

`tasks`（タスク）と `media`（ファイル情報）の2つが追加されています。

---

## 使い方

### タスク管理（経営ダッシュボード）

- 「＋ タスクを追加」から、やること・担当AI社員・期限・優先度を登録
- チェックボックスで完了・未完了を切り替え
- 「すべて / 進行中 / 完了 / 期限切れ」で絞り込み
- 期限を過ぎたタスクは、日付が赤く「期限切れ」と表示されます
- 完了率が上のバーに出ます

### ファイルのアップロード（クリエイティブスタジオ）

各パネルに、アップロード枠があります。

| 場所 | 入れるファイル |
| --- | --- |
| 音楽制作 | 音源（mp3・wav など） |
| 画像生成 | 画像（png・jpg など） |
| MV制作 | 動画（mp4 など） |
| ファイル管理 | 何でも（書類も含む） |

- **ドラッグ＆ドロップ**でも、クリックして選択でもアップロードできます
- 複数ファイルを一度に選べます
- サムネイルをクリックすると、大きく表示・再生できます
- 右上の ✕ で削除（Storage の実ファイルも一緒に消えます）
- 1ファイル **50MB** まで

MV制作パネルの一番上には、最後にアップロードした動画がそのまま再生できる形で表示されます。

---

## 保存容量について

Firebase Storage の無料枠は **5GB** です。
ファイル管理パネルに、いま何GB使っているかが表示されます。

- 画像（1枚 2MB）なら約2,500枚
- MV（1本 100MB）なら約50本

足りなくなったら、Firebaseの **Blazeプラン**（従量課金）に変更できます。
超えた分だけの課金なので、少量なら月数十円程度です。

---

## 見るだけモードのとき

Firebaseを設定していない状態でもアップロードを試せますが、
**ページを再読み込みするとファイルは消えます**（ブラウザの一時URLのため）。
その旨がアップロード枠の下に表示されます。

タスクのほうは、見るだけモードでも端末内に保存されるので消えません。

---

## うまくいかないとき

| 症状 | 確認すること |
| --- | --- |
| 「アップロードの権限がありません」 | `storage.rules` を貼り付けて「公開」したか |
| 同上 | ログイン中のメールアドレスが、ルールの一覧に入っているか |
| アップロード枠が出てこない | 中尾さん・シュンさんは閲覧のみです（社長アカウントでお試しください） |
| 「保存容量がいっぱいです」 | 不要なファイルを削除するか、Blazeプランへ変更 |
| 画像が表示されない | Storage のロケーション設定が完了しているか |
