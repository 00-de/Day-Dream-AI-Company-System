# フェーズ3：AI秘書をAIに接続する手順

AI秘書チャットが、実際のAI（Groq / Gemini / OpenAI）とつながります。
**APIキーはVercelのサーバー側だけが持ちます。ブラウザには一切出ません。**

---

## いちばん簡単な手順（Groqがおすすめ）

Groqは無料枠が大きく、返答がとても速いのでおすすめです。

### 1. APIキーを取得する

1. https://console.groq.com/keys を開く
2. Googleアカウントなどでログイン
3. 「Create API Key」を押す
4. 表示された文字列（`gsk_` で始まります）をコピー
   ※ この画面を閉じると二度と見られないので、必ずコピーしてください

### 2. Vercelに登録する

1. Vercel のプロジェクトを開く
2. **Settings → Environment Variables**
3. 次のとおり入力して「Save」

   | 項目 | 入れる値 |
   | --- | --- |
   | Key | `GROQ_API_KEY` |
   | Value | コピーしたキー（`gsk_...`） |
   | Environments | Production・Preview・Development すべてにチェック |

4. **Deployments → 最新のデプロイ → 「…」→ Redeploy**
   （環境変数は再デプロイしないと反映されません）

### 3. 確認する

アプリを開いて、ヘッダーの **設定** を押します。
「AI接続」の欄が **接続できます / Groq** になっていれば成功です。

AI秘書チャットの上のバッジも「Groq」に変わります。

---

## 大事な注意

**キー名に `VITE_` を付けないでください。**

`VITE_` が付いた環境変数は、ビルド時にブラウザ側のファイルへ埋め込まれます。
つまり誰でも見られる状態になり、キーを盗まれて高額請求につながります。

| 変数名 | 判定 |
| --- | --- |
| `GROQ_API_KEY` | ✅ 正しい（サーバー側だけ） |
| `VITE_GROQ_API_KEY` | ❌ 絶対にダメ（ブラウザに丸見え） |

Firebaseの6つの値（`VITE_FIREBASE_...`）は、公開されても問題ない値なので `VITE_` でOKです。

---

## 3つのAIを登録した場合の動き

登録したものを、上から順に試します。

1. **Groq**（`GROQ_API_KEY`）
2. **Gemini**（`GEMINI_API_KEY`）
3. **OpenAI**（`OPENAI_API_KEY`）

最初に成功したAIの答えを返します。
Groqが混雑や障害で失敗しても、自動でGeminiに切り替わるので、チャットは止まりません。

1つだけ登録した場合は、それだけを使います。

---

## APIキーを1つも登録しない場合

エラーにはなりません。**簡易応答モード**として、アプリ内のデータを見て答えます。

- 「今日の予定は？」→ 登録した予定を読み上げます
- 「売上を教えて」→ 保存した数字を答えます
- 「AI社員の稼働状況」→ 集計して答えます
- 「ライブ」「YouTube」「プロジェクト」「楽曲」にも対応

自由な会話や相談はできませんが、ダッシュボードの確認用としては充分に使えます。

---

## AI社員を選んで話せます

チャットの左上のプルダウンで、話す相手を選べます。

AI秘書 / AI社長 / AI戦略 / AI分析官 / AIマーケター / 悠真AI / 蓮AI / 結衣AI / 美琴AI / 高木

選んだAI社員の名前と担当が、AIへの指示文に反映されます。
たとえば蓮AIを選んで「新曲のアイデアが欲しい」と聞けば、音楽制作担当として答えます。

---

## AIが見ている情報

チャットのたびに、次の情報がAIに渡されます（保存中のデータそのままです）。

- 会社名・社長名
- 今月の売上・利益・目標・達成率・残り日数
- 今日の予定
- プロジェクトの進捗
- AI社員23人の名前・担当・稼働状況
- 次回ライブの日程・会場・準備進捗・残タスク
- YouTubeの登録者・再生回数・視聴時間
- 最近の楽曲・お知らせ

「データ編集」で数字を書き換えると、AIの答えもすぐにその数字に変わります。

---

## モデルを変えたいとき

Vercelの環境変数に、次を追加すると変更できます（任意）。

| 変数名 | 既定値 |
| --- | --- |
| `GROQ_MODEL` | `llama-3.3-70b-versatile` |
| `GEMINI_MODEL` | `gemini-2.0-flash` |
| `OPENAI_MODEL` | `gpt-4o-mini` |

---

## うまくいかないとき

| 症状 | 確認すること |
| --- | --- |
| 「簡易応答モード」のまま | 環境変数を登録した後、**Redeployしたか** |
| 同上 | キー名に `VITE_` が付いていないか |
| 同上 | Environments の3つすべてにチェックが入っているか |
| 「AIへの接続に失敗」 | キーの文字列が正しいか（前後の空白に注意） |
| OpenAIだけ失敗する | OpenAIは無料枠が無く、支払い設定が必要です |
